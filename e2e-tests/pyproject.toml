[project]
name = "e2e-tests"
version = "0.1.0"
description = "End-to-end tests for CustardSeed session sharing"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0.0",
    "playwright>=1.40.0",
    "pytest-playwright>=0.4.0",
]

[tool.pytest.ini_options]
testpaths = ["."]
